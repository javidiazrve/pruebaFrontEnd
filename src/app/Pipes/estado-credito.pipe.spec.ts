import { EstadoCreditoPipe } from './estado-credito.pipe';

describe('EstadoCreditoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoCreditoPipe();
    expect(pipe).toBeTruthy();
  });
});
