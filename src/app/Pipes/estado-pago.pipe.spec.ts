import { EstadoPagoPipe } from './estado-pago.pipe';

describe('EstadoPagoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoPagoPipe();
    expect(pipe).toBeTruthy();
  });
});
